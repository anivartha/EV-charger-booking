@'
# ⚡ EV Charger Booking System

A DBMS mini project simulating an EV charging slot booking system.

## 🧩 Overview
This project demonstrates a full-stack mini-application where passengers can book EV charger slots and admins can view station metrics. It uses PostgreSQL (with triggers and stored procedures), a TypeScript + Express backend, and a simple Tailwind + vanilla JS frontend.

## Tech Stack
- Frontend: HTML + TailwindCSS + Vanilla JS (served from `/public`)
- Backend: Node.js (Express) + TypeScript
- Database: PostgreSQL (stored procedures, triggers)

## Run locally
1. `npm install`
2. `npm run build`
3. `npm start`
Open: http://localhost:3000

## Repo layout
- /src      — TypeScript backend source
- /public   — frontend static files (index.html)
- package.json, tsconfig.json, .gitignore

## Authors
Anagha N Bharadwaj 
Anivartha Upadhyaya — DBMS Mini Project (PESU)
